version: '3.9'

services:
  qxgametool-server:
    image: qxgametool-server
    pull_policy: build
    restart: unless-stopped
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${EXTERNAL_SERVER_PORT}:80"
